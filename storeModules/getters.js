import { map, cloneDeep, keyBy } from 'lodash'

export default {
  // device: state => state.device || state.uaDevice,
  // uaDevice: state => state.uaDevice,
  // requestData: state => state.requestData,
  // gtm: state => state.gtm,
  // currentRestaurant: (state, getters, rootState) => {
  //   return getters.restaurants[rootState.route.params.restaurant]
  // }
}
